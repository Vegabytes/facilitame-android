import { Stack } from "expo-router";

export default function NotificacionesLayout() {
  return (
    <Stack
      screenOptions={{
        animation: "fade",
        headerShown: false,
      }}
    />
  );
}
