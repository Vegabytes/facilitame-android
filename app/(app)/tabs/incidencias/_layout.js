import { Stack } from "expo-router";

export default function IncidenciasLayout() {
  return (
    <Stack
      screenOptions={{
        animation: "fade",
        headerShown: false,
      }}
    />
  );
}
